spring:
  datasource:
    url: jdbc:h2:mem:rule_engine_db;MODE=MySQL;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  sql:
    init:
      mode: always
      encoding: UTF-8

logging:
  level:
    org.springframework.jdbc.core: WARN

